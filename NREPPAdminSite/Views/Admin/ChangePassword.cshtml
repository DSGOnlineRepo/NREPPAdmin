@model NREPPAdminSite.Models.ChangePasswordModel

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Title = "Change Password";
}

    
   
<div class="col-sm-6 col-sm-offset-3">
    @using (Html.BeginForm("ChangePassword", "Admin", new { returnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { @class = "form-horizontal" }))
    {
        @Html.AntiForgeryToken();
        <div class="col-md-12 m-b-15 p-l-0">
            <h2>@ViewBag.Title</h2>
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.CurrentPassword, new {@class = "col-md-4 control-label"})
            <div class="col-md-8">
                @Html.PasswordFor(m => m.CurrentPassword, new {@class = "form-control"})
            </div>
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.NewPassword, new {@class = "col-md-4 control-label"})
            <div class="col-md-8">
                @Html.PasswordFor(m => m.NewPassword, new { @class = "form-control" })
            </div>
        </div>
        <div class="form-group">
            @Html.LabelFor(m => m.ConfirmNewPassword, new { @class = "col-md-4 control-label" })
            <div class="col-md-8">
                @Html.PasswordFor(m => m.ConfirmNewPassword, new { @class = "form-control" })
            </div>
        </div>
        <div class="col-md-offset-3 col-md-9 p-l-0">
            <input type="submit" value="Change Password" class="btn btn-primary" />
        </div>

    }
    @Html.ValidationSummary()
</div>


