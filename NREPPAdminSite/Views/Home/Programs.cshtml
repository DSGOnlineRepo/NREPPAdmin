<!-- You will need to change the model once the programs become available -->
@model List<NREPPAdminSite.Models.Intervention>

@{
    ViewBag.Title = "Programs";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Welcome, @ViewBag.Fname</h2>

<div>
    <table>
        <tr>
            <th>Title</th>
            <th>Full Description</th>
            <th>Status</th>
            <th>Updated Date</th>
            <th>Submitter</th>
        </tr>

        @{
            foreach(NREPPAdminSite.Models.Intervention inv in Model)
            {
                <tr>
                    <!-- <td>@inv.Title</td> -->
                    <td class="withborder">@Html.ActionLink(inv.Title, "View", "Program", new {InvId = inv.Id }, new {id = "link" + inv.Id.ToString() }) </td>
                    <td class="withborder">@inv.FullDescription</td>
                    <td class="withborder">@inv.Status</td>
                    <td class="withborder">@inv.UpdatedDate.ToShortDateString()</td>
                    <td class="withborder">@inv.Submitter</td>
                </tr>
            }
        }
    </table>
    @Html.ActionLink("Create a New Submission", "View", "Program", new { InvId = "-1" }, null)<br />
    
</div>